import React from 'react'
import Prototype from './Prototype'

//So with the prototype i used the screensize of iphone 14 Pro MAX, so is a bit long, you have to scroll a little bit to have a full view of it
function App() {
  return (
    <div>
      <Prototype/>
    </div>
  )
}

export default App